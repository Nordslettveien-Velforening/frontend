# Nordslettveien frontend
This is a [Next.js](https://nextjs.org/docs/getting-started) application with React using Typescript.

### Run the app locally
Install dependencies

`yarn install` (first time only)

The start it

`yarn dev`
